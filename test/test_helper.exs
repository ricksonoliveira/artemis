ExUnit.start()

# Only start the database if we need it for tests that require it
# For this fuel calculator challenge, we'll skip database setup
# Ecto.Adapters.SQL.Sandbox.mode(Artemis.Repo, :manual)
