# Database disabled for fuel calculator challenge
# This module is kept as a stub to avoid compilation errors
defmodule Artemis.Repo do
  # use Ecto.Repo,
  #   otp_app: :artemis,
  #   adapter: Ecto.Adapters.Postgres
end
